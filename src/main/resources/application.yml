server:
  port: 8766

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tanzhengyang/config-client.git
          username:
          password:
          default-label: master
          search-paths: /**
      label: master


